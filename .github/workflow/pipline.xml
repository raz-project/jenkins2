name: My First Action In Git

on: push

jobs:
first-job:
  runs-on: ubuntu-latest
  steps:
    -name: checkout code
     run: git clone https://github.com/berezovsky13/nodejs.git
    -name: print name
     run: echo "hellow github Action"